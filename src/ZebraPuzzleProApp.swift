import SwiftUI

@main
struct ZebraPuzzleProApp: App {
    // We can initialize global services here if needed
    // let persistenceController = PersistenceController.shared

    var body: some Scene {
        WindowGroup {
            LandingView()
        }
    }
}
